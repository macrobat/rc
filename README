Keep conf files under version control.
ssh-agent zsh
ssh-add ~/.ssh/id_rsa_github
git log
git diff
git status
git add somefile
git commit -am "comment"
git push master origin

copy default file into rc, then
ln -sb file_in_rc default_name

On other machines you could copy from rc and modify if needed instead of
symlinking and keeping branches.

this creates a soft link and makes a backup default_name~ file
lollink() { lolname="$(readlink -nm "$1")"; ln -sb "$lolname" "${lolname/rc\/}"; }

leave some things out:
~/.ssh
~/.macromedia
~/.cache
~/.ccache


pick and choose:
games
	~/.xonotic
	~/.nexuiz
	~/.darkplaces
	~/.warsow
	~/.openarena

~/.emacs.d
~/.moc

~/.config/
	awesome
	openbox
	geeqie

have removed some weechat. trolltech
should remove some xfce4


merging
<angle< HEAD
old stuff
=equal=
new stuff
this creates a soft link and makes a backup default_name~ file
>angle> bla_sha625bc0bd06c73f44dmd5sum531a01fdd3

