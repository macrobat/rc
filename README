Keep conf files under version control.
ssh-agent zsh
ssh-add ~/.ssh/id_rsa_github
git log
git diff
git status
git add somefile
git commit -am "comment"
git push master origin

Different machines can branch.
copy default file into rc, then
ln -sb file_in_rc default_name
lollink() { naeme="$(readlink -nm "$1")"; ln -sb "$naeme" "${naeme/rc\/}"; }

leave some things out:
~/.ssh
~/.macromedia
~/.cache
~/.ccache
	

pick and choose:
games
	~/.xonotic
	~/.nexuiz
	~/.darkplaces
	~/.warsow
	~/.openarena

~/.emacs.d
~/.moc

~/.config/
	awesome
	openbox
	geeqie

